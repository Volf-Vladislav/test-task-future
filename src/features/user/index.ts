import userSlice, { setUsers, UserSlice, clearUsers, nextPage, switchLoading } from "./model/userSlice"
import RootState from "./model/RootState"

export { setUsers, userSlice, type UserSlice, clearUsers, nextPage, type RootState, switchLoading }  